<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SobuTenak" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!--         <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="CustomSwitchStyle">
        <item name="android:thumb">@drawable/thumb</item>
        <item name="android:track">@drawable/track</item>
        <item name="android:textOn">New User</item>
        <item name="android:textOff">Existing</item>
        <item name="android:textColor">#F82020</item>
    </style>

    <style name="Theme.SobuTenak" parent="Base.Theme.SobuTenak" />
</resources>